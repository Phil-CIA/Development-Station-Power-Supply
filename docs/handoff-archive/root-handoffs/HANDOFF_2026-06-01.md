# Development Station Power Supply - Handoff (2026-06-01)

## Session Closeout
This session is closed at user request.
Primary objective for this window was to lock in a stable display/telemetry baseline and leave a clean resume point.

## Stable Baseline At Close
1. Transport path is UART-only for HAT -> CrowPanel telemetry.
2. CrowPanel runtime is visually stable (no flicker/tearing reported in extended use).
3. Live values are responsive and charting is enabled with decimated updates.
4. Guarded flash workflow is available and verified for both targets.

## Runtime Settings To Preserve
1. HAT source cadence: 100 ms publish loop in src/i2c_scanner.cpp.
2. CrowPanel RGB timing in crowpanel-43-bringup/include/CrowPanel43Display.h:
   - freq_write = 16 MHz
   - vsync_back_porch = 16
3. CrowPanel UI/trend tuning in crowpanel-43-bringup/src/main.cpp:
   - kTrendSampleMs = 200
   - kChartDecimation = 2
   - kUiUpdateMinMs = 50
   - kVoltageUpdateDeadband_mV = 5
   - kCurrentUpdateDeadband_mA = 2

## Verification Snapshot
1. Build and upload paths were exercised for both targets during this stabilization cycle.
2. User reported multi-hour soak PASS with stable behavior.
3. No new regressions were reported at close.

## Tree Hygiene At Close
1. Handoff pointers updated to this file.
2. Session docs retained (soak/fault and export checklist) for continuity.
3. Safe generated build artifacts can be removed locally without affecting tracked source.

## Remaining Optional Item
1. Optional UART fault injection check (temporary unplug/replug) and result note.

## Next Session Quick Start
1. Read this file and docs/display-project/SOAK_AND_FAULT_TEST_2026-05-31.md.
2. Build and guarded-flash both targets once.
3. Confirm bench behavior: no flicker, responsive values, acceptable chart motion.
4. Only retune chart speed if explicitly requested.
